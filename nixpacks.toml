# nmap: used by passive recon (python-nmap). Use apt so we do not override Python's nixPkgs defaults.
[phases.setup]
aptPkgs = ["nmap"]

[start]
cmd = "python -m gunicorn api_server:app --bind 0.0.0.0:${PORT:-5000} --workers 2 --threads 4 --timeout 300"
