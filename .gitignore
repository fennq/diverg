# Environment and secrets (never commit — required for hackathon/public)
.env
.env.local
.env.*.local
.env.production
.env.staging
*.pem
*.key
secrets*.yml
secrets*.yaml
config.local.json
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]
*.so
.Python
*.egg-info/
.eggs/
dist/
build/

# Logs and runtime
*.log
bot.log

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Scan outputs and runtime data (keep private — do not commit)
results/
data/
reports/
*.brain.json
brain.json
content/*-results.json
content/*-scan-results.json
content/*proof-results.json

# Internal content and product docs (local only — do not commit)
content/
agents/
contracts/
PUBLIC.md
RECENT_UPDATES.md
scripts/generate_synq_report_pdf.py

# Investigation: Python modules are tracked; case outputs (reports, JSON) stay private
investigation/*
!investigation/*.py
!investigation/README.md
!investigation/bundle_intel_overrides.example.json
!investigation/bridge_programs_solana.json
!investigation/mixer_service_intel.json

# Local-only drafts (do not commit)
twitter/
node_modules/
